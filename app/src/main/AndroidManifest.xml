<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.worldskills.emparjapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.InicioActivity" />
        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.JugarActivity" />
        <activity android:name=".activities.ConfiguracionActivity" />
        <activity android:name=".activities.PuntajesActivity" />
        <activity android:name=".activities.NivelesActivity" />
        <activity android:name=".activities.JuegoDificilActivity" />
        <activity android:name=".activities.JuegoMedioActivity" />
        <activity android:name=".activities.JuegoFacilActivity" />
        <activity android:name=".activities.JuegoConfiguracionActivity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>